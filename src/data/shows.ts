export type Show = {
  date: string; // YYYY-MM-DD
  city: string;
  venue: string;
  country?: string;
  ticketUrl?: string;
  note?: string;
};

export const shows: Show[] = [
  // Neue Termine folgen.
];
